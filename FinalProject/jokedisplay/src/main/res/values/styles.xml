<resources>

    <!-- Base application theme. -->
    <style name="JokeTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="JokeTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="JokeTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="JokeTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
